# 🚀 Швидкий старт деплою

## Що потрібно зробити

### Варіант 1: Автоматичний деплой (рекомендовано)

1. **Запустити скрипт деплою:**
   ```bash
   ./deploy.sh
   ```

2. **Ввести пароль**, коли система запитає:
   ```
   FNrtVkfCRwgW
   ```

3. **Чекати завершення** (5-15 хвилин)

4. **Перевірити сайт**: https://foryou-realestate.com

### Варіант 2: Ручний деплой

Якщо автоматичний скрипт не працює, виконайте кроки з файлу `QUICK_DEPLOY.md`

## Що робить скрипт

1. ✅ Підключається до сервера
2. ✅ Встановлює Node.js, PM2, Nginx, Certbot
3. ✅ Створює директорію `/var/www/foryou-realestate`
4. ✅ Завантажує файли проекту
5. ✅ Створює `.env.local` з правильними змінними
6. ✅ Встановлює залежності (`npm install`)
7. ✅ Збирає проект (`npm run build`)
8. ✅ Налаштовує Nginx як reverse proxy
9. ✅ Запускає додаток через PM2
10. ✅ Налаштовує SSL сертифікат (Let's Encrypt)

## Якщо щось пішло не так

### Перевірити статус

```bash
# Підключитися до сервера
ssh root@135.181.201.185

# Перевірити PM2
pm2 status

# Перевірити логи
pm2 logs foryou-realestate

# Перевірити Nginx
systemctl status nginx
nginx -t
```

### Перезапустити додаток

```bash
ssh root@135.181.201.185
cd /var/www/foryou-realestate
pm2 restart foryou-realestate
```

### Переглянути детальні інструкції

Дивіться `QUICK_DEPLOY.md` для детальних інструкцій з усунення проблем.

## Після деплою

1. ✅ Перевірити, що сайт відкривається: https://foryou-realestate.com
2. ✅ Перевірити всі сторінки (properties, areas, map)
3. ✅ Перевірити, що фото завантажуються
4. ✅ Перевірити мобільну версію

## Важливо

- **Пароль сервера**: `FNrtVkfCRwgW` (змініть після деплою!)
- **Домен**: `foryou-realestate.com` (DNS вже налаштований)
- **Порт**: 3000 (внутрішній, через Nginx)
- **SSL**: автоматично налаштовується через Let's Encrypt

## Наступні кроки після деплою

1. Змінити пароль root на сервері
2. Налаштувати SSH ключі (без пароля)
3. Налаштувати резервне копіювання
4. Налаштувати моніторинг

---

**Готові до деплою? Запускайте:**
```bash
./deploy.sh
```

