# План оптимізації продуктивності - 122 запити на головній сторінці

## 🔍 АНАЛІЗ ПРОБЛЕМИ

### Поточний стан:
- **122 запити** при завантаженні головної сторінки
- Багато `.pbf` файлів (Mapbox тайли) - ~80-100 запитів
- `getPublicData()` завантажує ВСІ дані одразу (properties, areas, developers, facilities, courses)
- `getAreas()` завантажує ВСІ areas, хоча потрібні тільки 6
- Mapbox завантажується навіть якщо не видимий
- Немає lazy loading для компонентів
- Немає code splitting

### Джерела проблем:

1. **Hero компонент** (`components/Hero.tsx`):
   - Викликає `getPublicData()` → завантажує ВСІ дані (properties, areas, developers, facilities, courses)
   - Потрібні тільки areas для dropdown

2. **Areas компонент** (`components/Areas.tsx`):
   - Викликає `getAreas()` → завантажує ВСІ areas
   - Потрібні тільки 6 конкретних areas (TARGET_AREA_IDS)

3. **AboutHero компонент** (`components/AboutHero.tsx`):
   - Може завантажувати Mapbox навіть якщо карта не видима
   - Mapbox завантажує багато `.pbf` тайлів

4. **Mapbox тайли**:
   - ~80-100 `.pbf` файлів завантажуються навіть якщо карта не видима
   - Це найбільша частина запитів

---

## 🚀 РІШЕННЯ (від найпростіших до складних)

### 1. **Оптимізувати API запити на головній сторінці** ⚡⚡⚡⚡⚡
**Проблема:** `getPublicData()` та `getAreas()` завантажують зайві дані

**Рішення:**
- Замінити `getPublicData()` в Hero на `getAreas()` (тільки areas)
- Створити `getAreasByIds(ids: string[])` для завантаження тільки потрібних areas
- Використовувати кеш для areas

**Ефект:** ⚡⚡⚡⚡⚡ (дуже високий) - зменшення розміру відповіді на 70-90%
**Складність:** 🟢 Легко (2-4 години)
**Що потрібно:**
- Модифікувати `Hero.tsx` - замінити `getPublicData()` на `getAreas()`
- Модифікувати `Areas.tsx` - використовувати `getAreasByIds()` або фільтрувати на клієнті
- Додати кешування для areas

---

### 2. **Lazy Load компоненти, які не видимі відразу** ⚡⚡⚡⚡
**Проблема:** Всі компоненти завантажуються одразу, навіть якщо не видимі

**Рішення:**
- Використовувати `dynamic import` для компонентів нижче fold
- Використовувати `Intersection Observer` для lazy loading

**Ефект:** ⚡⚡⚡⚡ (високий) - зменшення початкового bundle на 40-60%
**Складність:** 🟢 Легко (3-5 годин)
**Що потрібно:**
- Lazy load `Areas`, `AboutUs`, `Partners`, `AboutSections` компоненти
- Використовувати `React.lazy()` або `dynamic()` з Next.js
- Додати Suspense boundaries

**Приклад:**
```tsx
const Areas = dynamic(() => import('@/components/Areas'), {
  loading: () => <AreasSkeleton />,
  ssr: false // або true, якщо потрібен SSR
});
```

---

### 3. **Lazy Load Mapbox тільки коли видимий** ⚡⚡⚡⚡⚡
**Проблема:** Mapbox завантажує ~80-100 `.pbf` файлів навіть якщо карта не видима

**Рішення:**
- Завантажувати Mapbox тільки коли компонент стає видимим
- Використовувати `Intersection Observer` для визначення видимості
- Використовувати `dynamic import` для Mapbox компонентів

**Ефект:** ⚡⚡⚡⚡⚡ (дуже високий) - зменшення запитів на 80-100
**Складність:** 🟡 Середньо (4-6 годин)
**Що потрібно:**
- Модифікувати `AboutHero.tsx` - завантажувати Mapbox тільки коли видимий
- Використовувати `Intersection Observer` для визначення видимості
- Додати skeleton для карти

---

### 4. **Оптимізувати Mapbox завантаження** ⚡⚡⚡
**Проблема:** Mapbox завантажує занадто багато тайлів одразу

**Рішення:**
- Налаштувати `maxZoom` та `minZoom` для обмеження тайлів
- Використовувати `maxBounds` для обмеження області
- Використовувати `lazyLoadTiles` опцію
- Кешувати тайли локально

**Ефект:** ⚡⚡⚡ (середній) - зменшення запитів на 30-50%
**Складність:** 🟡 Середньо (2-3 години)
**Що потрібно:**
- Налаштувати Mapbox опції в `MapboxMap.tsx`
- Додати `maxBounds` для Dubai
- Налаштувати `maxZoom` та `minZoom`

---

### 5. **Code Splitting для великих компонентів** ⚡⚡⚡
**Проблема:** Весь JavaScript завантажується одразу

**Рішення:**
- Використовувати `dynamic import` для великих компонентів
- Розділити bundle на менші частини

**Ефект:** ⚡⚡⚡ (середній) - зменшення початкового bundle на 30-50%
**Складність:** 🟢 Легко (2-3 години)
**Що потрібно:**
- Використовувати `dynamic()` для `AboutHero`, `MapboxMap`, `Areas`
- Додати loading states

---

### 6. **Оптимізувати кешування** ⚡⚡⚡
**Проблема:** Дані завантажуються повторно при кожному завантаженні

**Рішення:**
- Збільшити час кешування для статичних даних (areas, developers)
- Використовувати Service Worker для offline caching
- Використовувати IndexedDB для великих даних

**Ефект:** ⚡⚡⚡ (середній) - швидкість повторного завантаження +50-70%
**Складність:** 🟡 Середньо (4-6 годин)
**Що потрібно:**
- Збільшити `PUBLIC_DATA_CACHE_DURATION` з 5 хв до 30 хв для areas/developers
- Додати Service Worker (опціонально)

---

### 7. **Prefetch тільки критичних ресурсів** ⚡⚡
**Проблема:** Завантажуються ресурси, які не потрібні відразу

**Рішення:**
- Використовувати `prefetch` тільки для критичних ресурсів
- Видалити автоматичний prefetch для не-критичних

**Ефект:** ⚡⚡ (низький-середній) - зменшення початкового завантаження на 10-20%
**Складність:** 🟢 Легко (1-2 години)
**Що потрібно:**
- Перевірити `next.config.mjs` для prefetch налаштувань
- Використовувати `prefetch={false}` для не-критичних links

---

### 8. **Оптимізувати завантаження зображень** ⚡⚡⚡
**Проблема:** Зображення завантажуються одразу, навіть якщо не видимі

**Рішення:**
- Використовувати `loading="lazy"` для всіх зображень нижче fold
- Використовувати `Intersection Observer` для кастомного lazy loading
- Використовувати `priority` тільки для перших зображень

**Ефект:** ⚡⚡⚡ (середній) - зменшення початкового завантаження на 20-30%
**Складність:** 🟢 Легко (2-3 години)
**Що потрібно:**
- Додати `loading="lazy"` для зображень в `Areas`, `AboutUs`, `Partners`
- Використовувати `priority` тільки для Hero зображень

---

### 9. **Об'єднати API запити (Request Batching)** ⚡⚡
**Проблема:** Кілька окремих запитів замість одного

**Рішення:**
- Створити batch endpoint на бекенді
- Об'єднувати запити для areas + developers

**Ефект:** ⚡⚡ (низький-середній) - зменшення запитів на 2-3
**Складність:** 🔴 Складно (потрібен доступ до бекенду, 2-4 дні)
**Що потрібно:**
- Створити `/api/batch` endpoint на бекенді
- Об'єднувати запити для areas + developers

---

### 10. **SSR/ISR для статичних даних** ⚡⚡⚡⚡
**Проблема:** Всі дані завантажуються на клієнті

**Рішення:**
- Використовувати SSR для головної сторінки
- Використовувати ISR для areas/developers (оновлювати кожні 10 хв)

**Ефект:** ⚡⚡⚡⚡ (високий) - швидкість завантаження +40-60%
**Складність:** 🟡 Середньо (6-10 годин)
**Що потрібно:**
- Конвертувати `app/[locale]/page.tsx` в Server Component
- Використовувати `async function Page()` з `await getAreas()`
- Додати `revalidate` для ISR

---

## 📊 ПРОГНОЗОВАНІ РЕЗУЛЬТАТИ

### Після реалізації рішень 1-3 (найважливіші):
- ⚡ Запити: **122 → 20-30** (-75-85%)
- ⚡ Розмір відповіді API: **-70-90%**
- ⚡ Початковий bundle: **-40-60%**
- ⚡ Швидкість завантаження: **+50-70%**

### Після реалізації всіх рішень:
- ⚡ Запити: **122 → 15-25** (-80-90%)
- ⚡ Швидкість завантаження: **+70-90%**
- ⚡ Time to First Byte (TTFB): **-50-70%**
- ⚡ Lighthouse Score: **+30-50 points**

---

## 🎯 РЕКОМЕНДОВАНИЙ ПЛАН ДІЙ

### Фаза 1: Швидкі перемоги (1 день) ⚡
1. **Оптимізувати API запити** (1) - ⚡⚡⚡⚡⚡
2. **Lazy Load компоненти** (2) - ⚡⚡⚡⚡
3. **Lazy Load Mapbox** (3) - ⚡⚡⚡⚡⚡

**Очікуваний результат:** 122 → 30-40 запитів (-70-75%)

### Фаза 2: Додаткові оптимізації (2-3 дні) 🚀
4. **Оптимізувати Mapbox завантаження** (4) - ⚡⚡⚡
5. **Code Splitting** (5) - ⚡⚡⚡
6. **Оптимізувати кешування** (6) - ⚡⚡⚡
7. **Оптимізувати завантаження зображень** (8) - ⚡⚡⚡

**Очікуваний результат:** 30-40 → 20-25 запитів (-50-60%)

### Фаза 3: Довгострокові оптимізації (1 тиждень) 🎯
8. **SSR/ISR** (10) - ⚡⚡⚡⚡
9. **Prefetch оптимізація** (7) - ⚡⚡
10. **Request Batching** (9) - ⚡⚡ (якщо є доступ до бекенду)

**Очікуваний результат:** 20-25 → 15-20 запитів (-25-30%)

---

## 💡 ДОДАТКОВІ РЕКОМЕНДАЦІЇ

1. **Моніторинг:**
   - Додати Google Analytics для Core Web Vitals
   - Використовувати Lighthouse для тестування
   - Налаштувати alerts для повільних запитів

2. **Тестування:**
   - Тестувати на різних пристроях (mobile, tablet, desktop)
   - Тестувати на різних мережах (3G, 4G, WiFi)
   - Використовувати Chrome DevTools Network throttling

3. **Поступове впровадження:**
   - Починати з найпростіших оптимізацій
   - Тестувати кожну зміну окремо
   - Моніторити продуктивність після кожної зміни

---

## ❓ ПИТАННЯ ДЛЯ ВИБОРУ РІШЕНЬ

1. **Чи є доступ до бекенду?** (для рішення 9 - Request Batching)
2. **Які компоненти найважливіші?** (для пріоритизації lazy loading)
3. **Чи потрібен SSR?** (для рішення 10 - SSR/ISR)
4. **Який середній розмір areas/developers?** (для вирішення про кешування)

---

**Оберіть рішення, які хочете реалізувати, і я допоможу з імплементацією! 🚀**

